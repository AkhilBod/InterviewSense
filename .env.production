# This file contains environment variables for production deployment
# These values will be overridden by the actual environment variables set in Vercel dashboard

# Database - use a hosted PostgreSQL service like Vercel Postgres, Supabase, or Neon
DATABASE_URL=postgresql://postgres:InterviewSense@localhost:5432/myappdb?schema=public

# NextAuth Configuration
NEXTAUTH_URL=https://interview-sense-git-main-akhilbods-projects.vercel.app/
# The secret is set in Vercel environment variables (use the one from .env.local)

# Other API keys are set in Vercel environment variables
